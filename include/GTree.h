#include <bool.h>
#include <Node.h>

typedef struct {
    Node* root;
} GTree;

void* insert(GTree* tree, void* data);
void* contains(GTree* tree, void* data);
bool* insert(GTree* tree, void* data);
void* insert(GTree* tree, void* data);


